import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lavazlife.lavanotes',
  appName: 'Lava Notes',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;